namespace :deploy do
  desc "build site"
  after :updated, :build do
    on roles(:app) do
      within release_path do
        # execute :bundle, "exec middleman build"
        execute :ruby, "./script/update_cached_data.rb"
        execute :echo, '$SHELL'
        execute :touch, "deleteme"
      end
    end
  end
end
